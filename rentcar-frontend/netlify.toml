[build]
  command   = "npm run build"
  publish   = "dist"

[build.environment]
  NODE_VERSION = "20"

# React Router uchun — barcha routelar index.html ga yo'naltiriladi
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
